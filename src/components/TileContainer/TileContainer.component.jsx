// Import: Dependencies
import React from "react";

// Import: Elements
import { Container } from "./TileContainer.elements";

// Component: TileContainer
export default function TileContainer({ children }) {
  return <Container>{children}</Container>;
}
